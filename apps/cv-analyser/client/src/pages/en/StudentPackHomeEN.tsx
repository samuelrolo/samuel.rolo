// StudentPackHomeEN — thin wrapper that re-exports the unified page.
// All i18n is handled inside StudentPackHome via useTranslation().
import StudentPackHome from "../StudentPackHome";
export default StudentPackHome;
