diff --git a/dist/html2canvas.esm.js b/dist/html2canvas.esm.js
index dab56ad9b2319b51aea92c3f6cd8929ec8536e4a..f0d3bdbb688428907021d20330980d5dc7050f7f 100644
--- a/dist/html2canvas.esm.js
+++ b/dist/html2canvas.esm.js
@@ -1828,11 +1828,19 @@ var hsl = function (context, args) {
     var b = hue2rgb(t1, t2, h - 1 / 3);
     return pack(r * 255, g * 255, b * 255, a);
 };
+var oklch$1 = function (context, args) {
+    return pack(0, 0, 0, 0);
+};
 var SUPPORTED_COLOR_FUNCTIONS = {
     hsl: hsl,
     hsla: hsl,
     rgb: rgb,
-    rgba: rgb
+    rgba: rgb,
+    oklch: oklch$1,
+    oklab: oklch$1,
+    lab: oklch$1,
+    lch: oklch$1,
+    color: oklch$1
 };
 var parseColor = function (context, value) {
     return color$1.parse(context, Parser.create(value).parseComponentValue());
diff --git a/dist/html2canvas.js b/dist/html2canvas.js
index 84cb30d701da7baf13f14107fb28b3ea42db49f6..470daae592a5651abd1fb78f60bc021d985acd1c 100644
--- a/dist/html2canvas.js
+++ b/dist/html2canvas.js
@@ -1834,11 +1834,19 @@
         var b = hue2rgb(t1, t2, h - 1 / 3);
         return pack(r * 255, g * 255, b * 255, a);
     };
+    var oklch$1 = function (context, args) {
+        return pack(0, 0, 0, 0);
+    };
     var SUPPORTED_COLOR_FUNCTIONS = {
         hsl: hsl,
         hsla: hsl,
         rgb: rgb,
-        rgba: rgb
+        rgba: rgb,
+        oklch: oklch$1,
+        oklab: oklch$1,
+        lab: oklch$1,
+        lch: oklch$1,
+        color: oklch$1
     };
     var parseColor = function (context, value) {
         return color$1.parse(context, Parser.create(value).parseComponentValue());
diff --git a/dist/lib/css/types/color.js b/dist/lib/css/types/color.js
index 50608c326885d149ae29d3209652f37d828bc5fa..4d80dc39ecdbc9327807e343fbd52e3596936885 100644
--- a/dist/lib/css/types/color.js
+++ b/dist/lib/css/types/color.js
@@ -124,11 +124,26 @@ var hsl = function (context, args) {
     var b = hue2rgb(t1, t2, h - 1 / 3);
     return exports.pack(r * 255, g * 255, b * 255, a);
 };
+var oklch = function (context, args) {
+    // oklch is not natively supported by html2canvas.
+    // Return a fallback transparent color to prevent parsing errors.
+    // The actual visual rendering uses computed styles which browsers resolve to rgb.
+    return exports.pack(0, 0, 0, 0);
+};
+var lab = oklch;
+var lch = oklch;
+var oklab = oklch;
+var color_fn = oklch;
 var SUPPORTED_COLOR_FUNCTIONS = {
     hsl: hsl,
     hsla: hsl,
     rgb: rgb,
-    rgba: rgb
+    rgba: rgb,
+    oklch: oklch,
+    oklab: oklab,
+    lab: lab,
+    lch: lch,
+    color: color_fn
 };
 var parseColor = function (context, value) {
     return exports.color.parse(context, parser_1.Parser.create(value).parseComponentValue());
